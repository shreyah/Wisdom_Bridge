package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// -- Premium iOS-inspired Soft Pastel Warm Theme (Wisdom Bridge) --
val PrimaryBackgroundWarmWhite = Color(0xFFFAF7F5) // Warm White Background
val SecondarySurfaceSoftIvory = Color(0xFFF7F4F2)  // Soft Ivory Surface
val TextDeepCharcoal = Color(0xFF2C2625)           // Deep Charcoal (avoid pure black)
val BorderLightSystem = Color(0xFFEFE8E1)          // Warm faint line separator

// Accent Gradients & Pastel Tones
val BlushPink = Color(0xFFF8E7EE)                  // Blush Pink
val SoftLavender = Color(0xFFEADCF8)               // Soft Lavender
val SoftLilac = Color(0xFFDFE7FD)                  // Soft Lilac Highlight
val SoftWhiteGlass = Color(0xCCFFFFFF)             // Glassmorphic semi-transparent White

// Specialized Palette Cards
val PastelBlueCard = Color(0xFFE3F2FD)              // Expert sessions
val PastelPeachCard = Color(0xFFFFF2EC)             // Community events
val SoftGreenCard = Color(0xFFE8F5E9)               // Booked sessions
val LightGreyCard = Color(0xFFF0F1F2)               // Completed sessions

// Compatibility Aliases for entire codebase (mapping existing variables to the new premium iOS design system)
val PrimaryiOSBlue = Color(0xFF7D80C2)              // Lilac/Indigo Accent for primary interactive triggers
val PrimaryiOSCoral = Color(0xFFE19999)             // Muted Warm Coral
val AiryLightGrayBg = PrimaryBackgroundWarmWhite
val SolidCardWhite = Color(0xFFFFFFFF)              // Pure white premium card surfaces
val TextCharcoalDark = TextDeepCharcoal
val TextGraySub = Color(0xFF8F807A)                 // Soft warm gray for supplementary text
val AccentPeach = Color(0xFFEAA28A)                 // Soft peach accent
val ComfortSuccessGreen = Color(0xFF88C999)         // Pastel friendly green

val PrimarySleekTeal = PrimaryiOSBlue
val LighterSleekTeal = Color(0xFFB5B7E5)
val ActivePillTeal = Color(0xFFF2F0FB)
val SleekSandBeige = ComfortSuccessGreen
val SleekCharcoal = TextDeepCharcoal
val SleekRoseWhiteBg = PrimaryBackgroundWarmWhite
val SleekCardWhite = SolidCardWhite
val SupportingSandGray = Color(0xFFF5F2EF)
val ActiveLiveRed = Color(0xFFE57373)

val WisdomIndigo = Color(0xFF705DA3)
val SereneSage = Color(0xFF2E7D32)
val WarmBone = PrimaryBackgroundWarmWhite
val SoftSageContainer = Color(0xFFEBF7EE)

